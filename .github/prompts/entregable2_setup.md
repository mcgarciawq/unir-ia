# Prompt: Entregable 2 — AI Integration Kickoff

Read and execute **Phase 1** of `.github/plans/plan-2.md`.

1. Add `openai` and `python-dotenv` to `requirements.txt`.
2. Create project-root `.env` (Azure variables only) and extend `src/core/config.py` with `load_dotenv()` and `AZURE_OPENAI_*` constants.

Follow all rules in `.github/instructions/`.

Do **not** modify `.github/plans/plan-1.md`. Update progress only in `.github/plans/plan-2.md` after each validated step.

Stop after Phase 1 and wait for user OK before continue to the next steps in `.github/plans/plan-2.md`"